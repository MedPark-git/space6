PORT=8000
DATA_DIR=/app/user_data
# Cafe24 injects DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD.
# For local development only, use your PostgreSQL connection string.
DATABASE_URL=
OPENAI_API_KEY=
OPENAI_TEXT_MODEL=gpt-4.1-mini
OPENAI_TRANSCRIBE_MODEL=whisper-1
# Optional override for the shared access password.
SITE_PASSWORD=
